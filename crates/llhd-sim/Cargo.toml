[package]
name = "llhd-sim"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
clap = "2"
llhd = { path = "../.." }
log = { version = "0.4", features = ["release_max_level_info"] }
num = { version = "0.3", features = ["serde"] }
pretty_env_logger = "0.4"
rayon = "1.3"
