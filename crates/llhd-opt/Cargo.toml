[package]
name = "llhd-opt"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = "2"
llhd = { path = "../.." }
log = { version = "0.4", features = ["release_max_level_info"] }
pretty_env_logger = "0.4"
rayon = "1.3"
